# ── dependencies ──
node_modules/
.npm/

# ── build output ──
dist/
dist-ssr/

# ── env / secrets (ห้ามขึ้น git เด็ดขาด) ──
.env
.env.local
.env.*.local
deploy.bat

# ── Cloudflare Worker (deploy แยกผ่าน wrangler ไม่ต้องขึ้น git เว็บ) ──
meta-worker/

# ── logs ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── editor / OS ──
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
